apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: ec2-demo-unifort-location
  description: Location to import all ec2-demo-unifort catalog components
spec:
  targets:
    - ./.platform/*.yaml
